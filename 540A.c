#include <stdio.h>
#include <stdlib.h>

#define MAX_DISK 1001

void solve();

int main(int argc, char *argv[])
{
  solve();
  return EXIT_SUCCESS;
}

void solve() {
  int n;
  char current[MAX_DISK];
  char lock_comb[MAX_DISK];
  scanf("%s", current);
  scanf("%s", lock_comb);
  int moves = 0;
}
